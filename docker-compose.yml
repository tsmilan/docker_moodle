version: '3'

services:
  mahara:
    build: ./mahara
    ports:
      - "80:80"
    volumes:
      - ./siteroot:/siteroot
  mahara-db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mahara
      POSTGRES_USER: mahara_user
    volumes: 
      - 'maharadb:/var/lib/postgresql'
volumes:
    maharadb: